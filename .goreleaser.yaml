version: 2
project_name: kumabot

before:
  hooks:
    - go mod tidy

builds:
  - id: kumabot
    main: ./cmd/kumabot
    env:
      - CGO_ENABLED=1
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    skip: true

release:
  mode: replace
  draft: false
